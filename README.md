# minion — Pi Subagent Delegation Extension

Discover, list, and delegate tasks to specialized sub-agents from within
the [pi coding agent](https://github.com/earendil-works/pi). Ships four
built-in agents (explorer, scout, worker, reviewer) and supports user-defined
and project-level overrides.
